# Лабораторная работа №1
## Мазур София ИТ-15

### Задание 1

В этом файле были исправлены следующие ошибки:

1. **Разбиение на заголовочные и cpp-файлы**
   - Код был разделён на модули:
     - `Header.h` — объявления функций.
     - `Source.cpp` — реализация алгоритма.
     - `task1.cpp` — ввод/вывод и запуск программы.

2. **Подключения заголовочных файлов**
   - Добавлено `#include <algorithm>` для `std::max` и `std::swap`.
   - Добавлено `#include <locale>` для `setlocale`.
   - Добавлен `#include <limits>` для очистки потока ввода.
  
3. **Использование пространства имён**
   - Удалено `using namespace std;`.
   - Все обращения к стандартной библиотеке заменены на явные (`std::vector`, `std::ifstream`, `std::cout` и т.д.).
    
4. **Валидация пользовательского ввода**
   - Добавлена проверка корректности ввода через `std::cin.fail()`.
   - При ошибке поток ввода очищается с помощью `std::cin.clear()` и `std::cin.ignore()`.

5. **Именование**
   - Функции и переменные приведены к стилю PascalCase или информативные имена:
     - `insert_sort` → `InsertSort`
     - `backpack` → `CalculateBackpack`
     - `n`, `W` → `ItemCount`, `MaxWeight`
     - `dp` → `DynamicTable`
     - `temp` → `CurrentIndex`
     - `wt`, `val` → `Weight`, `Values`
   
6. **Циклы**
   - Префиксный инкремент (`++i`) вместо постфиксного в циклах `for`.

7. **Блоки `if-else`**
   - Однострочные блоки заменены на многострочные с фигурными скобками для лучшей читаемости.

8. **Фигурные скобки**
   - Исправлено расположение фигурных скобок по стандарту Epic Games C++ Codestyle.
  
### Задание 2

В этом файле были исправлены следующие ошибки:

1. **Разбиение на заголовочные и cpp-файлы**
   - Код был разделён на модули:
     - `Header.h` — объявления функций.
     - `Source.cpp` — реализация алгоритма.
     - `task2.cpp` — ввод/вывод и запуск программы.

2. **Подключение заголовочных файлов**
   - Добавлены недостающие заголовки:
     - `#include <algorithm>` для функции `std::reverse`.
     - `#include <clocale>` для функции `setlocale`.
     - `#include <limits>` для очистки потока ввода.

3. **Использование пространства имён**
   - Удалено `using namespace std;`.
   - Все обращения к стандартной библиотеке заменены на явные (`std::vector`, `std::ifstream`, `std::cout` и т.д.).

4. **Именование**
   - Переменные и функции приведены к информативным именам:
     - `n` → `boardSize`
     - `a` → `board`
     - `b` → `dynamicTable`
     - `put` → `pathTable`
     - `put1` → `resultPath`
     - `p` → `buildPath`
     - `max` → `maxValue`

5. **Глобальные переменные**
   - Удалена глобальная переменная `n`.
   - Размер доски теперь передается в функции через параметр `boardSize`.

6. **Циклы**
   - Использован префиксный инкремент (`++i`, `++j`) вместо постфиксного.

7. **Работа с файлами**
   - Добавлена проверка открытия файлов `a.txt` и `b.txt`.

8. **Форматирование**
   - Фигурные скобки и форматирование приведены к стилю Epic Games C++ Code Style.

### Задание 3

В этом файле были исправлены следующие ошибки:

1. **Подключение заголовочных файлов**
   - Добавлено `#include <clocale>` для функции `setlocale`.
   - Добавлен `#include <limits>` для очистки потока ввода.

2. **Использование пространства имён**
   - Удалено `using namespace std;`.
   - Все обращения к стандартной библиотеке заменены на явные (`std::cout`, `std::cin`, `std::endl`).
     
3. **Валидация ввода**
   - Реализована защита от ввода символов и строк вместо чисел.
   - При возникновении ошибки состояние потока `std::cin` очищается с помощью `std::cin.clear()`.
   - Лишние символы во входном буфере удаляются через `std::cin.ignore(std::numeric_limits<std::streamsize>::max(), '\n')`.
   - Ввод повторяется до тех пор, пока пользователь не введет корректные значения.

4. **Проверка диапазонов**
   - Добавлена проверка условий задачи:
     - `2 ≤ K ≤ 10`
     - `1 < N < 20`
     - `K + N < 26`
   - При нарушении условий пользователю выводится сообщение об ошибке и выполняется повторный ввод.

3. **Именование**
   - Переменные приведены к информативным именам:
     - `nz` → `nonZeroEndingCount`
     - `oz` → `oneZeroEndingCount`
     - `tz` → `twoZeroEndingCount`
     - `_nz` → `previousNonZero`
     - `_oz` → `previousOneZero`
     - `_tz` → `previousTwoZero`
     - `s` → `result`

4. **Циклы**
   - Использован префиксный инкремент (`++i`) вместо постфиксного.

5. **Приведение типов**
   - Добавлено явное приведение к `double` через `static_cast` при вычислении результата.

6. **Завершение программы**
   - Добавлен `return 0;` в конце функции `main()`.

### Задание 4
  
1. **Разбиение на заголовочные и cpp-файлы**
   - Код был разделён на модули:
     - `Header.h` — объявления функций.
     - `Source.cpp` — реализация алгоритма.
     - `task4.cpp` — ввод/вывод и запуск программы.
2. **Использование пространства имён**
   - Удалено `using namespace std;`.
   - Все обращения к стандартной библиотеке заменены на явные (`std::cout`, `std::cin`, `std::endl`).

3. **Отдельные файлы**
   - `Header.h` — объявления функций.
   - `.cpp` — реализация функций.
   - `main.cpp` — точка входа, ввод/вывод и запуск логического выражателя.

4. **Использование пространства имён**
   - Удалено `using namespace std;`.
   - Все обращения к стандартной библиотеке заменены на явные (`std::cout`, `std::cin`, `std::endl`).
     
5. **Именование**
   - Информативные имена функций и переменных:
     - `evaluate` → `evaluateExpression`
     - `SplitRecursive` → `splitRecursive`
     - `s` → `input`
     - `exprs` → `expression`
     - `Params` → `params`
     - `Pos` → `position`

6. **Форматирование**
   - Фигурные скобки и форматирование приведены к стилю Epic Games C++ Code Style.
  
7. **Возврат из main**
   - Добавлен `return 0;` в конце `main()`.
